{% extends 'base.html.twig' %}


{% block body %}

    <div class="container my-5">
        

        <div class="row">
            
            
             <div class="col-md-6 text-center">
               
              <img src="/uploads/{{ product.image }}" class="img-fluid" alt="{{ product.name }}">
            </div>

             <div class="col-md-6 text-center">INFO 2</div>

            {{ dump(product) }}
           
            
        </div>

    </div>

{% endblock %}

